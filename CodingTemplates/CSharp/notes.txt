Initialize from repo directory (e.g., CodingTemplates, etc.):
dotnet new console -o CSharp

Must include the SQLite package using the following command:
cd CSharp
dotnet add package System.Data.SQLite.Core

To run from terminal, run from repo directory (e.g., CodingTemplates, etc.):
dotnet run --project CSharp